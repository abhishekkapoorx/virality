PORT=4010

# Same as api — queue/worker uses Postgres + Redis from Docker Compose for local dev.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/linkedin_agent
REDIS_URL=redis://localhost:6379
