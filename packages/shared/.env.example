# packages/shared is a library — it does not read process env at runtime.
# Copy DATABASE_URL / REDIS_URL from api or worker when wiring integration tests later.
# Canonical definitions:
#   ../../api/.env.example
#   ../../worker/.env.example
