NEXT_PUBLIC_API_URL=http://localhost:4000

# Web app talks to the API; it does not connect to Postgres/Redis directly in the MVP plan.
# Documented here so local env stays consistent with api/worker (see ../api/.env.example).
# DATABASE_URL=
# REDIS_URL=
